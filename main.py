# name = input("What is your name?")
# Variable assignment seems to be different than in JS and similar to Ruby
# print(name)

#A Simpler way to apply the solution from the last video.
name = input("What is your name? ")
length = len(name)
print(length)